---
- name: System Setup
  hosts: MacOS
  tags: system
  pre_tasks:
    - name: Gather facts
      ansible.builtin.setup:
      tags: always
  roles:
    - role: user
      tags: user
    - role: homebrew
      tags: homebrew
    - role: zsh
      tags: zsh
    - role: ansible
      tags: ansible

- name: Development Environment
  hosts: MacOS
  tags: dev
  pre_tasks:
    - name: Gather facts
      ansible.builtin.setup:
      tags: always
  roles:
    - role: java
      tags: java
    - role: go
      tags: go
    - role: cpp
      tags: cpp
    - role: python
      tags: python

- name: IDEs
  hosts: MacOS
  tags: ides
  pre_tasks:
    - name: Gather facts
      ansible.builtin.setup:
      tags: always
  roles:
    - role: vscode
      tags: vscode

- name: Infrastructure
  hosts: MacOS
  tags: infra
  pre_tasks:
    - name: Gather facts
      ansible.builtin.setup:
      tags: always
  roles:
    - role: kubernetes
      tags: kubernetes
