---
# Packages for Fedora (RedHat family)
python_fedora_packages:
  - python3
  - python3-pip
  - python3-devel
  - python3-virtualenv

# Packages for macOS (Darwin family)
python_macos_packages:
  - python

# Optional list of global pip packages to install
python_pip_packages: []
